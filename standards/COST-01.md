# COST-01 Right-sizing and idle resources
Batch and non-production workloads must be scheduled or autoscaled so they do not run idle.
GPU shapes need a written justification.
Every compartment must have a budget with alerts.
Keywords: cost, batch, schedule, autoscale, idle, shape, budget, always-on
